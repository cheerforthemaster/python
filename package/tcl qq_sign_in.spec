# -*- mode: python -*-

block_cipher = None


a = Analysis(['The\\AppData\\Local\\Programs\\Python\\Python36\\tcl qq_sign_in.py'],
             pathex=['C:\\Users"Master', 'C:\\Users\\Master The\\PycharmProjects\\package'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='tcl qq_sign_in',
          debug=False,
          strip=False,
          upx=True,
          console=False )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='tcl qq_sign_in')
